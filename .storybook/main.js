module.exports = {
    stories: [
        "../src/**/*.stories.@(ts|tsx)"
    ],
    framework: '@storybook/react',
    addons: [
        "@storybook/addon-links",
        "@storybook/addon-essentials"
    ],
}
